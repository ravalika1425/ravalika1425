export interface dest {
    attributes: Object,
    morphTargetDictionary: Object,
    morphAttributes: Object,
    morphTargetInfluences : Object,
    index: any[]
}

export interface ThreeMesh{
    meshes: (THREE.Mesh[] | THREE.SkinnedMesh)[]

}
